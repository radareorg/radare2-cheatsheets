\usepackage[utf8]{inputenc}
\usepackage[
	a4paper,
	foot=10pt,
	left=45pt,
	right=45pt,
	top=30pt,
	bottom=30pt,
	landscape
]{geometry}
\usepackage[dvipsnames]{xcolor}
\usepackage{multicol}
\usepackage{fancyhdr}
\usepackage[yyyymmdd]{datetime}
\usepackage[ colorlinks=false, hidelinks]{hyperref}

\renewcommand{\dateseparator}{}
\renewcommand{\dots}{\ \dotfill{}\ }
\newcommand{\cmd}[2]{{\color{MidnightBlue}{\tt #1}}{\color{lightgray}~\dotfill{}~}#2\\}
\DeclareTextFontCommand{\ttc}{\color{MidnightBlue}\ttfamily}
\setlength{\columnsep}{30pt}

\makeatletter
    \edef\gitepoch{\@@input|"echo [$(git log -n1 --format=\@percentchar ct \jobname.tex)]"}
\makeatother

\fancypagestyle{r2fancy} {
	\fancyhf{}
	\renewcommand{\headrulewidth}{0pt}
	\fancyfoot[R]{\footnotesize{\tt {\color{gray} \gitepoch}}}
	\fancyfoot[L]{\footnotesize{\tt \href{http://radare.org}{radare.org}}}
}

